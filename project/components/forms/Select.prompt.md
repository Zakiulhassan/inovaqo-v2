Native select for discovery-call forms. Budget options start at "Under $30K" — never lower.

```jsx
<Select label="Budget range" options={["Under $30K", "$30K–$75K", "$75K–$150K", "$150K+", "Not decided yet"]} />
```
