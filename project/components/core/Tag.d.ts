import * as React from "react";

/** Quiet mono capability tag (LLM, RAG, CLOUD…). Always lowercase-key, renders uppercase. */
export interface TagProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
