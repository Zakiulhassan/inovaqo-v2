Mono uppercase pill for states and emphasis; pair the dot with a label (never color-only).

```jsx
<Badge tone="signal" dot>Live</Badge>
<Badge tone="neutral">AI Product</Badge>
<Badge tone="alert" dot>Action needed</Badge>
```

Use `signal` sparingly — lime is the active state, not decoration.
